@model GaragesQueryModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Index">Garage</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Model.TopRatedGarage
            @*    @Html.DisplayNameFor(model => model.Garages)*@
            </th>
            <th>@Model.TotalGaragesCount</th>
           @*@foreach (var item in Model.)
            {
                <th>@item.Name</th>
                <th>@item.Owner</th>
                <th>@item.Rating</th>
            }*@
            <th></th>
        </tr>
    </thead>
    <tbody>
@*@foreach (var item in Model.Garages) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Owner)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Owner">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Owner">Details</a> |*@
@*                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
*@            </td>
        </tr>
}
    </tbody>
</table>
